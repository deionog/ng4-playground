import { HeatDirective } from './heat.directive';

describe('HeatDirective', () => {
  it('should create an instance', () => {
    //const directive = new HeatDirective();
    //expect(directive).toBeTruthy();
  });
});
